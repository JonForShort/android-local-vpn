dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "com.github.jonforshort"

project(":vpn").name = "android-local-vpn"

include ':app'
include ':vpn'
